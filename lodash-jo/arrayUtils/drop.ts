export function drop(array:any[],n:number):any[]{
  const result:any[] = []
  const length = result.length
  if(n<length){
    length +1 - n
  }
  else{
    return array.splice(0,length -2)
  }
  return 
}
